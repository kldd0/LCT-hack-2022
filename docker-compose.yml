version: '3.8'

services:
  backend:
    build: ./app/backend
    ports:
      - 8000:8000
    volumes:
      - ./app/backend:/app
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 8000